/**
 * The login page uses its own minimal layout (no header/footer/providers).
 */
export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
